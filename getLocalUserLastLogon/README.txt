This script produces a CSV file with the last logon of each local user of a list of servers.

Can be useful for example if you are planning to implement LAPS to ensure the local administrator of domain machines is not actually used.